
  #cloud:
    #config:
   #   profile: config
      #uri: http://192.168.169.128:8080/my.config.server/
     # uri: http://192.168.0.107:8080/my.config.server
      #name: my.order
      #label: dev
#server:
  #port: 8888

spring:
  #application:
    #name: my.order
  cloud:
    config:
      discovery:
        service-id: my.config.server
        enabled: true
      profile: config
      name: my.order
      #fail-fast: true
eureka:
  client:
    service-url:
      defaultZone: http://192.168.169.128:8080/my.demo/eureka/
  instance: 
      prefer-ip-address: true